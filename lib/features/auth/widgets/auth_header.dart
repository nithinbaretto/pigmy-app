import 'package:flutter/material.dart';

/// Auth feature widgets placeholder.
class AuthHeader extends StatelessWidget {
  const AuthHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
