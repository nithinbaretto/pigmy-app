import 'package:flutter/material.dart';

/// Summary widgets placeholder.
class SummaryChart extends StatelessWidget {
  const SummaryChart({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
